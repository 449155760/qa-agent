# 智能 QA 内容审核 Agent 需求分析

## 1. 项目背景

企业在客服、知识库问答、RAG 助手、内部制度问答和售前咨询等场景中，越来越多地使用大模型生成回答。生成式问答提升了响应效率，但也带来新的内容风险：

- 回答可能包含违规、敏感、歧视、暴力、自伤等风险内容。
- 回答可能泄露个人信息、账号信息、合同信息或内部数据。
- 回答可能缺少证据支撑，产生事实幻觉。
- 回答可能超出业务授权范围，例如承诺价格、 SLA、法律责任或医疗建议。
- 回答可能答非所问，或与知识库证据冲突。

因此需要一个“智能 QA 内容审核 Agent”，在问答内容交付给用户前，进行分层审核、风险识别、人工复核路由和审计记录。

## 2. 建设目标

构建依托 LangGraph 的多节点审核 Agent，实现：

1. 对问答输入进行结构化解析。
2. 对问题和答案进行安全、隐私、业务、事实一致性审核。
3. 对风险进行分级评分。
4. 对不同风险等级自动路由到通过、重写、拒绝或人工复核。
5. 输出完整审核报告，支持审计与追溯。
6. 为后续接入 LLM、知识库、工单系统和人工审核台预留接口。

## 3. 用户角色

| 角色 | 诉求 |
|---|---|
| 业务运营 | 希望问答内容稳定、合规、可解释 |
| 客服团队 | 希望高风险回答能被拦截或转人工 |
| 风控/合规 | 希望规则可配置、过程可审计 |
| 算法/研发 | 希望审核链路模块化、可扩展、可评测 |
| 管理者 | 希望看到风险分布、误杀率、漏检率和效率指标 |

## 4. 输入范围

基础输入：

- `case_id`：审核任务 ID。
- `question`：用户问题。
- `answer`：候选回答。
- `evidence`：知识库检索证据或来源片段。
- `business`：业务线。
- `metadata`：渠道、用户类型、会话上下文、模型版本等。

## 5. 输出范围

输出审核报告：

- `decision`：`approve`、`reject`、`rewrite`、`human_review`。
- `risk_level`：`low`、`medium`、`high`、`critical`。
- `score`：0 到 100。
- `issues`：命中的规则与原因。
- `suggested_action`：处理建议。
- `safe_answer`：可选，重写后的安全回答。
- `audit_trace`：节点执行记录。

## 6. 功能需求

### 6.1 输入校验

- 校验问题和答案是否为空。
- 校验证据是否存在。
- 标准化字段结构，去除异常空白字符。

### 6.2 安全审核

- 识别违法违规内容。
- 识别暴力、色情、自伤、仇恨辱骂等风险。
- 识别诱导绕过规则、攻击系统提示词、越权请求。

### 6.3 隐私审核

- 识别身份证号、手机号、邮箱、银行卡号等个人信息。
- 判断答案是否输出未经授权的个人信息。
- 对隐私风险进行高优先级拦截。

### 6.4 业务审核

- 判断答案是否答非所问。
- 判断答案是否做出超授权承诺。
- 判断答案是否存在绝对化承诺，例如“永久免费”“保证成功”“一定赔付”。

### 6.5 事实与证据审核

- 判断答案是否可由证据支持。
- 识别与证据冲突的陈述。
- 当证据不足时标记为“需要人工复核”或“需要重写”。

### 6.6 风险分级与路由

- `low`：可通过。
- `medium`：建议重写或补充证据。
- `high`：进入人工复核。
- `critical`：直接拒绝或强制人工审核。

## 7. 非功能需求

- 可解释：每个审核结论必须有命中规则和原因。
- 可配置：规则、阈值、业务线策略可配置。
- 可追溯：保留审核链路、节点结果和版本信息。
- 可扩展：可接入 LLM 分类器、RAG 证据校验、人工审核台。
- 可评测：支持离线数据集批量运行，统计误杀率、漏检率、召回率。

## 8. 验收标准

- 能对输入问答生成结构化审核报告。
- 能识别典型隐私泄露、越权承诺、证据不足和非法 JSON/空字段。
- 能按风险级别输出路由决策。
- 能保留节点执行轨迹。
- 本地规则版本可运行，LangGraph 编排版本可集成。
